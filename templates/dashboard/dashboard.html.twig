{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <div class="flex flex-col space-y-16 mt-14 pl-10">
        <div class="w-80 h-36">
            <div class="inline-flex flex-col items-center justify-end flex-1 h-full bg-white shadow">
                <div class="w-full h-1/3">
                    <div class="flex-1 h-full bg-white shadow">
                        <p class="flex-1 h-full text-2xl px-8 font-semibold leading-9 text-center text-gray-400">Total Bannis</p>
                    </div>
                </div>
                <p class="w-full h-2/3 pt- text-7xl text-center text-blue-500">{{ count }}</p>
            </div>
        </div>
    </div>
{% endblock %}
